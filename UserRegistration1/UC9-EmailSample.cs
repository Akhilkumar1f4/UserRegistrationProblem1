public class EmailSampleClearer
{
    public void ClearAllEmailSamples(List<string> emailSamples)
    {
        if (emailSamples == null)
        {
            throw new ArgumentNullException(nameof(emailSamples));
        }

        // Perform the necessary action to clear the email samples
    }
}
